#!/usr/bin/env sh
# Commit-msg hook — validates the commit message against Conventional Commits.
# Allowed types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert, spec.

npx --no-install commitlint --edit "$1"
